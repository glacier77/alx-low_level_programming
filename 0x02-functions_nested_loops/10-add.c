#include "main.h"
/**
 * add - adds two int and return the result
 * @a: int added to b
 * @b: int added to a
 * Return: sum of a and b
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
