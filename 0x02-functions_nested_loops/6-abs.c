#include "main.h"
/**
 * _abs - Computes the absolute value of an int
 *
 * @i: input the number as integer
 *
 * Return: absolute value
 */
int _abs(int)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
